package cyder.login;

/** The valid login modes. */
enum LoginMode {
    /** Expecting general input. */
    INPUT,

    /** Expecting a username. */
    USERNAME,

    /** Expecting a password. */
    PASSWORD
}
