package cyder.bounds;

/** The type of string a given String is. */
public enum TaggedStringType {
    /** The string type is an HTML tag. */
    HTML,

    /** The string type is regular text. */
    TEXT
}
